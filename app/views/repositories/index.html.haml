#sidebar
  #repo_main
    %h1= "My Repositories #{link_to 'new repo', new_repository_path}"
    #repo_links
      %ul
        - @repositories.each do |r|
          %li= link_to r.name, tree_path(@current_user.login, r.name, 'master')
#content
  %h1 Updates Feed
